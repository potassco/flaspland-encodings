# Benchmarking experiments
2026-04-29

These are the raw output files collected during our initial benchmarking for ASPOCP and for LPNMR. 
The results are included in the `flaspland-encodings/benchmarking/analysis` directory. 
If for some reason information needs to be extracted from these files, place them back into the `flaspland-encodings/benchmarking` directory and invoke the necessary commands from the `btool`.